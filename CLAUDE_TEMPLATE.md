# CLAUDE.md 模板

本文件为 Claude Code (claude.ai/code) 提供代码库操作指南。

## 项目概述

<!-- 简要描述项目的用途、技术栈 -->
[项目名称] 是一个基于 [语言/框架] 的 [项目类型]。主要功能包括 [核心功能列表]。

**技术栈**：
- 语言：[Go/Python/Node.js/...]
- 框架：[Gin/Echo/FastAPI/Express/...]
- 数据库：[MySQL/PostgreSQL/SQLite/MongoDB/...]
- 其他：[Redis/消息队列/...]

## 常用命令

```bash
# 开发运行
[启动命令]

# 构建
[构建命令]

# 测试
[测试命令]

# 代码质量检查
[lint/format 命令]

# 依赖管理
[依赖安装/更新命令]
```

## 架构

### 目录结构

```
项目根目录/
├── main.go (或 index.js, app.py)    入口文件
├── cmd/                              命令行工具（如有）
├── internal/                         内部包（不对外暴露）
├── pkg/                              公共包
├── api/                              API 定义
│   ├── handler/                      请求处理器
│   ├── middleware/                   中间件
│   └── router/                       路由注册
├── model/                            数据模型
├── service/                          业务逻辑层
├── repository/                       数据访问层
├── config/                           配置管理
├── utils/                            工具函数
└── docs/                             文档
```

### 关键模式

<!-- 描述项目中的重要设计模式和约定 -->

**路由规范**：
- API 前缀：`/api/v1`
- RESTful 风格命名

**响应格式**：
```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

**错误处理**：
- [描述错误处理方式]

**数据库约定**：
- [表命名规范]
- [迁移方式]

### 核心模块说明

<!-- 列出核心模块及其职责 -->

| 模块 | 职责 | 关键文件 |
|------|------|----------|
| 认证 | 用户登录、JWT | `auth/` |
| 用户 | 用户 CRUD | `user/` |
| ... | ... | ... |

## 环境与配置

### 环境变量

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `PORT` | 服务端口 | `8080` |
| `DB_DSN` | 数据库连接 | - |
| `JWT_SECRET` | JWT 密钥 | - |

### 配置文件

- 开发环境：`config/dev.yaml`
- 生产环境：`config/prod.yaml`

## 开发指南

### 添加新接口

1. 在 `handler/` 下创建处理函数
2. 在 `router/` 中注册路由
3. 添加相应的 service 和 repository 层代码
4. 编写单元测试

### 数据库迁移

```bash
[迁移命令]
```

### 测试规范

- 单元测试文件命名：`*_test.go`
- 测试覆盖率要求：[百分比]

## 部署

### Docker

```bash
docker build -t [镜像名] .
docker run -p 8080:8080 [镜像名]
```

### 其他部署方式

[描述其他部署方式]

## 注意事项

<!-- 列出开发时需要注意的点 -->
- [注意事项 1]
- [注意事项 2]

---

> 提示：根据实际项目情况填写和调整以上内容，删除不适用的部分。